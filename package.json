{
  "name": "runde.tips",
  "license": "MIT",
  "version": "0.0.6",
  "scripts": {
    "dev": "pnpm --filter www run dev",
    "build": "pnpm --filter www run build",
    "start": "pnpm --filter www run start",
    "prisma": "pnpm --filter www run prisma",
    "icons": "pnpm --filter www run icons",
    "check": "biome check --write",
    "typecheck": "pnpm --filter www run typecheck"
  },
  "engines": {
    "node": "20"
  },
  "packageManager": "pnpm@9.6.0",
  "devDependencies": {
    "@biomejs/biome": "1.8.3"
  }
}
