{
  "name": "@haus23/runde.tips",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "pnpm run --filter www start",
    "db:push": "turbo db:push db:generate",
    "db:studio": "pnpm run --filter console db:studio",
    "icons:add": "pnpm run --filter ui icons:add",
    "icons:build": "pnpm run --filter ui icons:build",
    "check": "biome check --apply ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.1",
    "changeset-conventional-commits": "^0.2.4",
    "turbo": "^1.12.3"
  }
}
